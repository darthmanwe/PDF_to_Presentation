# Secrets — never commit environment files
.env
.env.*
*.env
!.env.example
# The frozen desktop app's filled key file (real keys, injected at build time
# into dist/). The empty packaging/config.template.txt stays tracked as the example.
pdfdeck.config.txt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Build / distribution artifacts
build/
dist/
*.spec
!packaging/*.spec
*.rar

# Generated output
output/
runs/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
